#ifndef PROBES_H
#define PROBES_H

#define STARTED_RAY_INTERSECTION(ray)
#define FINISHED_RAY_INTERSECTION(ray, isect, ht)
#define STARTED_RAY_INTERSECTIONP(ray)
#define FINISHED_RAY_INTERSECTIONP(ray, hit)

#endif // PROBES_H